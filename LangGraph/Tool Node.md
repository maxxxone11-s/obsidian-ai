---
type: concept
area: LangGraph
status: learned
created: 2026-06-26
updated: 2026-06-27
tags:
  - langgraph
  - tool
  - action
confidence: 0.96
---

# Tool Node

Узел для выполнения инструмента/функции в LangGraph.

## Интуитивное объяснение

Как рука робота, которая берёт нужный инструмент и выполняет действие.

## Кратко

Tool — внешняя функция, Tool Node — Node, вызывающий Tool.

## Простое объяснение

Tool Node — это специальный узел, который выполняет внешний инструмент или функцию.

Tool похож на service в FastAPI, а Tool Node встраивает его в граф.

## Зачем это нужно

- Выполнение функций/инструментов
- Интеграция с внешними системами
- Расширение возможностей агента

Позволяет агенту работать с внешним миром: API, базами данных, файлами и вычислениями.

## Как это работает

### Механизм
- Получает информацию о инструменте из State
- Выполняет инструмент
- Обновляет State с результатом

[[Tool]] описывает действие, а [[Tool Node]] получает команду на вызов инструмента и возвращает результат в [[State]].

## Пример

```python
def tool_node(state):
    # Выполняем инструмент
    result = execute_tool(state["tool_call"])
    state["results"].append(result)
    return state

graph.add_node("tools", tool_node)
```

`calories_tool()` считает калории для рецепта.

## Типичные ошибки

- Забывают обработать ошибки инструмента
- Неправильное обновление State
- Бесконечное выполнение инструментов

- Считать Tool Node самим Tool.

## Вопросы для проверки

- Чем отличается Tool от Tool Node?

## Следующие темы

- [[LLM Tool Calling]]

## Связанные темы

- [[Node]] — тип узла
- [[State]] — передаёт информацию о инструменте
- [[Router]] — выбирает tool node
- [[Conditional Edge]] — условие для tool node

- [[LLM Tool Calling]]
